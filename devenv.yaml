# Nix inputs (flake sources)
inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixpkgs-unstable
  # Provides Python versions dropped from nixpkgs-unstable (e.g. 3.10 after EOL)
  nixpkgs-python:
    url: github:cachix/nixpkgs-python
    inputs:
      nixpkgs:
        follows: nixpkgs

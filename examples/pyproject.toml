[project]
name = "examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "metaxy",
  "duckdb>=1.4.1",
]

[tool.uv.sources]
metaxy = { workspace = true }

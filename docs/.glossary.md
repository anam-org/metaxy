*[prunable update]: An update that skips change propagation through certain dependency graph paths because it modifies fields that downstream features don't depend on.

*[prunable updates]: Updates that skip change propagation through certain dependency graph paths because they modify fields that downstream features don't depend on.

*[partial data update]: An update that skips change propagation through certain dependency graph paths because it modifies fields that downstream features don't depend on.

*[partial data updates]: Updates that skip change propagation through certain dependency graph paths because they modify fields that downstream features don't depend on.

*[prunable partial update]: An update that skips change propagation through certain dependency graph paths because it modifies fields that downstream features don't depend on.

*[prunable partial updates]: Updates that skip change propagation through certain dependency graph paths because they modify fields that downstream features don't depend on.

*[root feature]: A feature without any upstream (parent) features.

*[Polars versioning engine]: The local versioning engine using polars-hash with Polars optimizations like projection pruning and predicate pushdown. Reference implementation for Metaxy.

*[polars versioning engine]: The local versioning engine using polars-hash with Polars optimizations like projection pruning and predicate pushdown. Reference implementation for Metaxy.

*[feature discovery]: Automatic searching for feature definitions in the current environment. | /guide/concepts/feature-discovery/

*[Metaxy configuration]: Settings via metaxy.toml, pyproject.toml, or environment variables. | /reference/configuration/

*[`metaxy.toml` file]: Configuration file for Metaxy settings. | /reference/configuration/

*[configuration file]: Settings file (metaxy.toml or pyproject.toml) for Metaxy. | /reference/configuration/

*[config discovery]: Automatic searching for Metaxy configuration files. | /reference/configuration/#config-discovery

*[configuration discovery]: Automatic searching for Metaxy configuration files. | /reference/configuration/#config-discovery

*[fields versions]: User-defined versions for logical components of a single feature sample. | /guide/concepts/definitions/features/#fields

*[field version]: User-defined version for a logical component of a single feature sample. | /guide/concepts/definitions/features/#fields

*[field versions]: User-defined versions for logical components of a single feature sample. | /guide/concepts/definitions/features/#fields

*[field-level dependencies]: Dependencies between specific fields of feature definitions that affect how field versions are computed. | /guide/concepts/definitions/features/#field-level-dependencies

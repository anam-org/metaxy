*[prunable update]: An update that only changes a given piece of data partially in a way that doesn't affect some of the downstream dependencies. | /guide/concepts/data-versioning/#example-partial-data-updates

*[prunable updates]: An update that only changes a given piece of data partially in a way that doesn't affect some of the downstream dependencies. | /guide/concepts/data-versioning/#example-partial-data-updates

*[partial data update]: An update that only changes a given piece of data partially in a way that doesn't affect some of the downstream dependencies. | /guide/concepts/data-versioning/#example-partial-data-updates

*[partial data updates]: An update that only changes a given piece of data partially in a way that doesn't affect some of the downstream dependencies. | /guide/concepts/data-versioning/#example-partial-data-updates

*[prunable partial update]: An update that only changes a given piece of data partially in a way that doesn't affect some of the downstream dependencies. | /guide/concepts/data-versioning/#example-partial-data-updates

*[prunable partial updates]: An update that only changes a given piece of data partially in a way that doesn't affect some of the downstream dependencies. | /guide/concepts/data-versioning/#example-partial-data-updates

*[root feature]: A feature without any upstream (parent) features.

*[Polars versioning engine]: The local versioning engine implemented in Polars (and polars-hash). Reference versioning engine implementation for Metaxy.

*[polars versioning engine]: The local versioning engine implemented in Polars (and polars-hash). Reference versioning engine implementation for Metaxy.

*[feature discovery]: Automatic searching for feature definitions in the current environment. | /guide/concepts/projects/#feature-discovery

*[Metaxy configuration]: Settings via metaxy.toml, pyproject.toml, or environment variables. | /reference/configuration/

*[`metaxy.toml` file]: Configuration file for Metaxy settings. | /reference/configuration/

*[configuration file]: Settings file (metaxy.toml or pyproject.toml) for Metaxy. | /reference/configuration/

*[config discovery]: Automatic searching for Metaxy configuration files. | /reference/configuration/#config-discovery

*[configuration discovery]: Automatic searching for Metaxy configuration files. | /reference/configuration/#config-discovery

*[fields versions]: User-defined versions for logical components of a single feature sample. | /guide/concepts/definitions/features/#fields

*[field version]: User-defined version for a logical component of a single feature sample. | /guide/concepts/definitions/features/#fields

*[field versions]: User-defined versions for logical components of a single feature sample. | /guide/concepts/definitions/features/#fields

*[field-level lineage]: Dependencies between specific fields of feature definitions that affect how field versions are computed. | /guide/concepts/definitions/features/#field-level-lineage

*[external feature definitions]: Feature definitions referencing Metaxy features defined in other Metaxy projects, typically not available in the current Python environment. | /guide/concepts/definitions/features/#external-features

*[external features]: Feature definitions referencing Metaxy features defined in other Metaxy projects, typically not available in the current Python environment. | /guide/concepts/definitions/features/#external-features

*[external feature definition]: Feature definition referencing an Metaxy feature defined in other Metaxy projects, typically not available in the current Python environment. | /guide/concepts/definitions/features/#external-features

*[external feature]: Feature definition referencing an Metaxy feature defined in other Metaxy projects, typically not available in the current Python environment. | /guide/concepts/definitions/features/#external-features

*[multi-environment setups]: Metaxy codebases which have more than one distinct Python environment. | /guide/concepts/projects/#multi-environment-setups

*[metaxy.lock]: A file generated by Metaxy to track external feature definitions. | /guide/concepts/definitions/external-features#metaxy-lock-file

*[fully qualified field path]: A unique identifier for a field within a feature definition, consisting of the feature name and the field name. Example: `/raw/video:frames`.

*[syntactic sugar]: Simplified syntax for common Metaxy constructs. | /guide/concepts/syntactic-sugar
